# Backend (FastAPI inference service) — runtime deps only.
#
# Lock policy: pin everything with ==. These are starting pins; once the team
# installs in the shared env, run `pip freeze > backend/requirements.txt` to
# lock the exact resolved versions (same env used to train/serialize the model
# — sklearn/xgboost pickles are NOT portable across versions).
#
# Heavy training deps (astroquery, the DL framework for *training*, etc.) belong
# in scripts/requirements.txt, NOT here — keep the serving image slim.

# --- web / serving ---
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20        # multipart/form-data uploads (the cutout file)
pydantic==2.10.4

# --- arrays / model I/O ---
numpy==2.2.1

# --- model storage: load MODEL_URI from gs:// ---
google-cloud-storage==2.19.0

# --- baseline model runtime (ships first) ---
scikit-learn==1.6.1
xgboost==2.1.3

# --- optional, enable when needed ---
# torch==2.5.1                   # when the *served* model is the CNN
# astropy==7.0.1                 # if /predict accepts FITS cutouts (not just .npy)
